# СГЕНЕРИРОВАН — не правьте руками, правьте ../requirements.txt
#
# Копия списка зависимостей рядом с точкой входа: сборщик Python на Vercel
# ищет requirements.txt именно здесь. Ссылка «-r ../requirements.txt» не
# сработала — установка запускается не в том каталоге, и относительный путь
# не разрешается.
#
# Совпадение с ../requirements.txt проверяется в CI: разойтись молча они не
# смогут, а это единственная опасность копии.
fastapi>=0.115
uvicorn[standard]>=0.34
pydantic[email]>=2.10
pydantic-settings>=2.7
sqlalchemy>=2.0.36
psycopg[binary]>=3.2
alembic>=1.14
pyjwt>=2.10
bcrypt>=4.2
# Чтение файлов Excel при импорте справочников
openpyxl>=3.1

# Клиент для вызовов Telegram Bot API
httpx>=0.28
python-multipart>=0.0.20
# S3-совместимое хранилище файлов. Нужен, только если задан S3_BUCKET,
# импортируется лениво.
boto3>=1.35
