# Список зависимостей рядом с точкой входа.
#
# Сборщик Python на Vercel ищет requirements.txt в каталоге точки входа и в
# корне проекта. Корневой файл не сработал: функция развернулась без библиотек
# и падала с ModuleNotFoundError: No module named 'fastapi'. Этот файл
# закрывает второе место поиска.
#
# Список не дублируем — ссылаемся на настоящий. pip разворачивает вложенную
# ссылку относительно каталога этого файла, поэтому расхождению взяться неоткуда.
-r ../requirements.txt
